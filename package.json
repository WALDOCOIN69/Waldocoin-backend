{
  "name": "waldocoin-backend",
  "version": "1.0.0",
  "description": "Backend for WALDOcoin using JSON Server",
  "scripts": {
    "start": "npx json-server --watch db.json --port $PORT"
  },
  "dependencies": {
    "json-server": "^0.17.3"
  }
}
